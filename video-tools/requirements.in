mcp[cli]>=1.0,<2.0
httpx>=0.27,<1.0
Pillow>=10.0
numpy>=1.26
librosa>=0.10
# Floor numba so uv never backtracks into ancient llvmlite (<3.10-only) to
# satisfy a too-new numpy; numpy resolves down to numba's cap instead.
numba>=0.60
soundfile>=0.12
# The headless distribution of PySceneDetect (same `scenedetect` module, on
# opencv-python-headless, no libGL). 0.7.1 is the first line that accepts
# click 8.3.3, the fix for PYSEC-2026-2132; 0.6.x pins click below 8.3.
scenedetect-headless>=0.7.1
opencv-python-headless>=4.9
starlette
uvicorn
# Motion-graphics rendering (render_motion_clip): deterministic HTML→frames
# via headless Chromium. The image installs chromium-headless-shell.
playwright>=1.45,<2
